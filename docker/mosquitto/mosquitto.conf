# Desenvolvimento local — ajuste permissões e allow_anonymous em produção
# Escuta em todas as interfaces (LAN + Docker); senão o ESP pode não alcançar o broker.
listener 1883 0.0.0.0
protocol mqtt
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
