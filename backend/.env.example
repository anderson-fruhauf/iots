# PostgreSQL (a senha com caracteres especiais deve ser codificada na URL: # -> %23)
DATABASE_URL=postgresql://postgres:SENHA@191.252.182.41:5432/iot?sslmode=disable

# Crie a tabela na primeira vez (dev); em produção prefira migrations e deixe false
DATABASE_SYNC=true

# Opcional: só se o servidor exigir SSL
# DATABASE_SSL=true

MQTT_URL=mqtt://127.0.0.1:1883

PORT=3000
